# ==========================================
# 환경 변수 설정 예시
# ==========================================
# 이 파일을 복사해서 .env.local로 만들어 사용하세요

# ==========================================
# 로컬 개발 환경 (localhost)
# ==========================================
# API Rewrite Target - 로컬 백엔드 서버 주소 (개발 환경)
API_REWRITE_TARGET=http://localhost:8000

# API Base URL - 비워두면 상대 경로 사용 (Next.js rewrite 적용)
NEXT_PUBLIC_API_BASE_URL=

# ==========================================
# 프로덕션 환경 (Vercel 등)
# ==========================================
# API Rewrite Target - 설정하지 않음 (rewrite 비활성화)
# API_REWRITE_TARGET=

# API Base URL - 백엔드 서버의 실제 URL (절대 경로)
# NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com

# ==========================================
# 참고사항
# ==========================================
# 로컬 개발: API_REWRITE_TARGET 설정 + NEXT_PUBLIC_API_BASE_URL 비우기
# 프로덕션: API_REWRITE_TARGET 비우기 + NEXT_PUBLIC_API_BASE_URL 설정
